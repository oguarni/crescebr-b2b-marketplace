Run frontend tests with coverage and report results.

## Steps

1. Run: `cd frontend && npx vitest run --coverage`
2. Report:
   - Pass/fail summary
   - Any failing tests with error messages
   - Coverage table focusing on files below 50%
   - Recommendations for improving coverage

## Arguments

- `$ARGUMENTS` - Optional: specific test file pattern to filter
